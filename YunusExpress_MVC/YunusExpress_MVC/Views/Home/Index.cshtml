@{
    ViewData["Title"] = "Home Page";
    Layout = null;
}


<!DOCTYPE html>
<html lang="az">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <style>
        #content{
            display:none;
        }
    </style>
    <title>YunisExpress</title>
    <link rel="stylesheet" href="~/assets/style/index.css">
    <link rel="stylesheet" href="~/assets/style/reset.css">
    <link href="~/assets/favicon.ico" rel="icon" />
</head>

<body>

    <div id="content">
    <div class="container">
        <div class="main-card">
            <div class="logo-container">

                <img src="~/assets/logo.png" alt="YunisExpress Logo" class="logo-image">
                <h1 class="brand-name">YunisExpress</h1>
            </div>

            <div class="option-card">
                <h3 class="card-title">Yeni Sifariş</h3>
                <a asp-action="Create" asp-controller="Order" class="btn-primary">Başla</a>
            </div>


            <div class="option-card">
                <h3 class="card-title">Tapmaq və Dəyişmək</h3>
                <a asp-action="Index" asp-controller="Order" class="btn-primary">Axtar</a>
            </div>


            <div class="option-card">
                <h3 class="card-title">Kuryerlərim</h3>
                <a asp-controller="Courier" asp-action="CourierHomePage" class="btn-primary">Keç</a>
            </div>
            <div class="option-card">
                <h3 class="card-title">Hesabatlar</h3>
                <a href="javascript:void(0);" onclick="checkPassword()" class="btn-primary">Keç</a>
            </div>

        </div>
    </div>
    </div>
</body>

</html>

<script>
    // window.addEventListener('DOMContentLoaded', () => {
    //     setTimeout(() => {
    //         const correctPassword = "1234";
    //         let userPassword;

    //         while (true) {
    //             userPassword = prompt("Zəhmət olmasa şifrəni daxil edin:");

    //             if (userPassword === null) {
    //                 document.body.innerHTML = "<h2>Access Canceled</h2>";
    //                 return;
    //             }

    //             if (userPassword === correctPassword) {
    //                 document.getElementById("content").style.display = "block";
    //                 break;
    //             } else {
    //                 alert("Şifrə yanlışdır. Yenidən Cəhd edin");
    //             }
    //         }
    //     }, 100); Small delay (100ms)
    // });
    window.addEventListener('load', () => {
        // Check if password already entered this session
        if (sessionStorage.getItem("access_granted") === "true") {
            document.getElementById("content").style.display = "block";
            return;
        }

        setTimeout(() => {
            const correctPassword = ""; // 🔑 Change to your desired password YaAllah
            let userPassword;

            while (true) {
                userPassword = prompt("Zəhmət olmasa şifrəni daxil edin:");

                if (userPassword === null) {
                    document.body.innerHTML = "<h2>Şifrə yanlışdır.Giriş imtina edildi</h2 > ";
                    return;
                }

                if (userPassword === correctPassword) {
                    sessionStorage.setItem("access_granted", "true");
                    document.getElementById("content").style.display = "block";
                    break;
                } else {
                    alert("Şifrə yanlışdır.Giriş imtina edildi.");
                }
            }
        }, 100); // Small delay for better compatibility
    });
    function checkPassword() {
        const correctPassword = ""; //🔑 Change to your real password yunis05032022
        const input = prompt("Zəhmət olmasa şifrəni daxil edin:");

        if (input === correctPassword) {
            window.location.href = "/Home/Hesabatlar";
        } else if (input !== null) {
            alert("Şifrə yanlışdır. Giriş imtina edildi.");
        }
        
    }
    

</script>